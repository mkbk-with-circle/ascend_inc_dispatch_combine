# 昇腾样机
## 去掉前同步
**无显式前同步，前同步应该与通信算子耦合在一起**
对于**allgather和reducescatter算子**，我们增加一个flag表示启动前同步；
开启flag后
1. 各个rank的首包会带上pre_sync标记（或者单独一个flag包），表示启动前同步
2. INC识别各个rank的pre_sync标记，在集齐所有的flag之前缓存所有的包，集齐之后开始**均匀**发送所有的包；
3. 暂时不考虑反压机制


## 组播
1. 设计协议，由各个src rank发一个控制消息，然后建立连接，组播.. 需要**均匀**发送；先建立好；
2. 先bcast，然后allgather
3. 流量分chunk，尽可能实现pipeline从而把带宽尽可能打满



## 网内归约
1. 流量分chunk
2. 先设计reduce，再reducescatter
   + 对于chunk 0,1,2....，当所有rank的chunk i都集齐后立刻计算并且将计算结果转发；
   + 需要设计好pipeline

## 设计上的一些注意事项
1. 对于组播，以及在网计算，其首包显然都需要携带一些控制消息，用于指示是哪些组参与；这种应该在最开始的时候就初始化，类似于nccl中对ep、dp、tp group的初始化，然后在每次需要进行ag或者rs的时候，就可以快速知道哪些组进行通信了；
2. 基于[shmem](ascend-样机/shmem)的通信来设计


## 收益点
1. 前同步直接省掉
2. ag与rs交叠时可以节省带宽
   + ag中，每个rank从发n-1收n-1变成发1收n-1（因为由INC复制n-1份发生），节省上行带宽
   + rs中，每个rank从发n-1收n-1变成发n-1收1（因为由INC完成reduce计算），节省下行带宽




## 要求
先在1INC节点上实现8NPU+1INC（这样会成为），然后实现8NPU+8INC；
在1INC节点上测试前同步的开销省略，在8INC节点上测试组播和在网计算的收益；



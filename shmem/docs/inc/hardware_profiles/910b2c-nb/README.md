# 910b2c-nb

Candidate hardware profile for `npu-borrow`.  The authoritative environment
facts and qualification state are in `single_inc_ENV_STATUS.md`.

This profile deliberately does not inherit roofline values or physical
placements from `910b-yuanmingyu`.

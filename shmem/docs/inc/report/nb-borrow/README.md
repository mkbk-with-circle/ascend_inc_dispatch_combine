# nb-borrow Pull V2 结果

- [`pull_v2_directional_20260909`](pull_v2_directional_20260909/README.md)：
  当前唯一性能报告；Dispatch 使用 fan-out 下行字节，Combine 使用归约上行字节。

旧 GET+PUT 相加口径的报告已删除，可从 Git 历史回退。当前树只保留汇总、口径和
复现命令，不提交逐 case JSON、PE 日志或构建产物。
